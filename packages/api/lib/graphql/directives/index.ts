import { IsAuthenticatedDirective } from './isAuthenticated'
import { ValidationDirective } from './validation'

export default {
  isAuthenticated: IsAuthenticatedDirective,
  validation: ValidationDirective,
}
